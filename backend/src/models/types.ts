export interface Movie {
    id: number; // We'll generate this as a unique identifier
    name: string; // Movie Name
    genre: string; // Genre
    year: number; // Year of Release
    imageUrl: string; // Link to Movie Image
}